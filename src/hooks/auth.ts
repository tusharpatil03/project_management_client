// import { useContext } from 'react';
// import AuthContext from '../components/AuthProvider/AuthProvider'

// /* this func is used to provide extrected values from authcontext,
// hence the inner logic remain  and we can use auth states by only calling this function */

// const useAuth = () => {
//     const { auth, setAuth } = useContext(AuthContext);
//     return { auth, setAuth };
// };

// export default useAuth;